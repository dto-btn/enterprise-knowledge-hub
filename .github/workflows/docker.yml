name: Docker (Build/Push)

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

env:
  IMAGE_NAME: enterprise-knowledge-hub
  REGISTRY: ghcr.io
  # MAX_JOBS x NVCC_THREADS = effective compile parallelism. Each MAX_JOBS unit
  # is a separate nvcc process that can use several GB of RAM compiling
  # flash-attn's CUDA templates, so keep this low relative to the runner's RAM
  # (not just CPU cores) to avoid OOM-killing the runner/host. See Dockerfile.
  MAX_JOBS: 4
  NVCC_THREADS: 1

jobs:
  build:
    name: Build ${{ matrix.label }} image
    runs-on: [self-hosted, ssc-dto]
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: CPU
            base_image: cpu
            cuda_arch: ""
            build_flash: "FALSE"
            platforms: linux/amd64,linux/arm64
            tag_suffix: ""
          # Temporarily disabled flash-attn even in CUDA builds due to failure (lack of power imo...) in runners.
          - label: CUDA H100/H200
            base_image: cuda
            cuda_arch: "9.0"
            build_flash: "FALSE"
            platforms: linux/amd64
            tag_suffix: "-cuda-9.0"
          - label: CUDA L40
            base_image: cuda
            cuda_arch: "8.9"
            build_flash: "FALSE"
            platforms: linux/amd64
            tag_suffix: "-cuda-8.9"
    steps:
      - uses: actions/checkout@v4

      - name: Extract version from pyproject.toml
        id: version
        shell: bash
        run: |
          set -euo pipefail
          VERSION=$(grep -m1 '^version' pyproject.toml | sed -E 's/version = "(.*)"/\1/')
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Validate tag matches pyproject version
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: |
          set -euo pipefail
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          if [ "${TAG_VERSION}" != "${{ steps.version.outputs.version }}" ]; then
            echo "Tag version (${TAG_VERSION}) does not match pyproject.toml version (${{ steps.version.outputs.version }})."
            exit 1
          fi

      - name: Install GoC CA cert
        run: |
          set -euo pipefail
          sudo mkdir -p /etc/docker/certs.d/ghcr.io
          awk '1' certs/GOC-GDC-ROOT-A.crt certs/GOC-GDC-ISSUING-A1A.crt | sudo tee /etc/docker/certs.d/ghcr.io/ca.crt > /dev/null

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
        with:
          buildkitd-config-inline: |
            [registry."ghcr.io"]
              ca=["/etc/docker/certs.d/ghcr.io/ca.crt"]

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          platforms: ${{ matrix.platforms }}
          build-args: |
            BASE_IMAGE=${{ matrix.base_image }}
            CUDA_ARCH=${{ matrix.cuda_arch }}
            MAX_JOBS=${{ env.MAX_JOBS }}
            NVCC_THREADS=${{ env.NVCC_THREADS }}
            BUILD_FLASH=${{ matrix.build_flash }}
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository }}:${{ steps.version.outputs.version }}${{ matrix.tag_suffix }}
            ${{ env.REGISTRY }}/${{ github.repository }}:sha-${{ github.sha }}${{ matrix.tag_suffix }}
