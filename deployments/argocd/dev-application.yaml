apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ekh-dev
  namespace: ekh-dev
spec:
  project: default
  source:
    repoURL: https://github.com/dto-btn/enterprise-knowledge-hub.git
    targetRevision: main
    path: deployments/ekh
    helm:
      releaseName: dev
      valueFiles:
        - values.yaml
        - values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: ekh-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
